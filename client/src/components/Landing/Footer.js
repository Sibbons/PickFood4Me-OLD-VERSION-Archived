import React, { Component } from 'react';

class Footer extends Component {

    render() {
        return (
            <div>
                <div className="footer">
                    Only Displays Open Locations
                </div>
            </div>

        );
    }
}

export default Footer;